# Remake of Uplife (old bachelor's project)

Probably the only thing i will keep from before is the db.

## Project Setup
First install dependencies
```npm i```
To run locally in dev
```npm run dev```